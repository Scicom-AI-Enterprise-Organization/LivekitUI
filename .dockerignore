.git
.next
node_modules
data/
*.png
*.log

# Sandbox node_modules built in their own stage
example/agent-starter-react/node_modules
example/agent-starter-react/.next
example/meet/node_modules
example/meet/.next

# Python venv built in its own stage
example/agent-starter-python/venv
example/agent-starter-python/.venv
example/agent-starter-python/__pycache__

# Secrets — never bake these into a layer. Gitignored, so CI checkouts don't
# have them, but a local `docker build` from a working tree would.
.env
.env.local

# Dev files
Dockerfile
docker-compose.yml
README.md
CLAUDE.md
.gitignore
.dockerignore
